(()=>{
/* Wine Blind V11 RESET
   Historical V10.7.2 UX is preserved; this patch replaces only diagnostic rendering with the frozen C2-C2 engine
   and applies the validated UX changes. No C2-C2 coefficient is changed here. */
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const E=()=>window.WineBlindEngine;
const C=()=>window.WINE_BLIND_CANDIDATE;
const AXIS_LABELS={"Acidité":"Acidité","Tanins":"Tanins","Alcool":"Alcool","Corps":"Corps","Intensité visuelle":"Couleur"};
const NUM={"faible":1,"faibles":1,"pale":1,"pâle":1,"moyenne−":2,"moyenne-":2,"moyens−":2,"moyens-":2,"moyen−":2,"moyen-":2,"leger":1,"léger":1,"moyenne":3,"moyen":3,"moyens":3,"moyenne+":4,"moyens+":4,"moyen+":4,"elevee":5,"élevée":5,"eleves":5,"élevés":5,"corse":5,"corsé":5,"intense":5};
const GROUP_MAP={
 "Fruits noirs":['family','Fruits noirs'],"Fruits rouges":['family','Fruits rouges'],"Agrumes":['family','Agrumes'],"Fruits verts":['family','Fruits verts'],"Fruits à noyau":['family','Fruits à noyau'],"Fruits exotiques":['family','Fruits exotiques'],
 "Floral":['signature','Floral'],"Végétal":['signature','Végétal / herbacé'],"Herbes":['signature','Végétal / herbacé'],"Épices fortes":['signature','Épicé / poivré']
};
const n=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[’']/g,"'").trim();
const fmt=x=>Number(x).toFixed(1).replace('.',',');
const sat=x=>x<1.75?'Faible':x<2.75?'Moyen−':x<3.5?'Moyen':x<4.25?'Moyen+':'Élevé';
function grapeTypeMap(){const m={};(window.WSET_DATA?.grapes||[]).forEach(g=>m[g.name]=g.type);return m}
function exactLookup(){const out=new Map();const aliases=C()?.engine?.observation_extraction?.C2_exact_aliases||{};Object.entries(aliases).forEach(([item,arr])=>{out.set(n(item),item);(arr||[]).forEach(a=>out.set(n(a),item))});return out}
function readObservation(){
 const structure={};
 $$('#structureFields .sat-field').forEach(f=>{const lab=f.querySelector('.sat-label')?.textContent?.trim(),axis=AXIS_LABELS[lab];if(!axis)return;const raw=f.querySelector('.sat-value')?.textContent?.trim();if(!raw)return;let v=NUM[n(raw)];if(v!=null)structure[axis]=v});
 const families=new Set(),signatures=new Set(),exacts=new Set(),lookup=exactLookup();
 $$('.aroma-group.selected > .aroma-chip').forEach(b=>{const hit=GROUP_MAP[b.textContent.trim()];if(hit)(hit[0]==='family'?families:signatures).add(hit[1])});
 $$('.descriptor-chip.active').forEach(b=>{const item=lookup.get(n(b.textContent));if(item)exacts.add(item)});
 return {structure,families:[...families],signatures:[...signatures],exacts:[...exacts],coherenceTokens:[]};
}
function filteredScore(){const eng=E();if(!eng)return null;const obs=readObservation(), scored=eng.score(obs), type=$('#typeWhite')?.classList.contains('active')?'Blanc':'Rouge',tm=grapeTypeMap();const grapes=scored.grapes.filter(r=>!tm[r.profile.grape]||tm[r.profile.grape]===type);const profiles=scored.profiles.filter(r=>!tm[r.profile.grape]||tm[r.profile.grape]===type);return {obs,grapes,profiles}}
function idxScore(I){return Math.max(0,Math.min(100,Math.round((I/4.2)*100)))}
function why(r){const chips=[];(r.st?.detail||[]).filter(d=>d.inRange).slice(0,3).forEach(d=>chips.push({t:`✓ ${d.axis.toLowerCase()} ${fmt(d.x)}`}));(r.ar?.matchedExacts||[]).slice(0,3).forEach(x=>chips.push({t:`✓ ${x}`,exact:true}));(r.st?.detail||[]).filter(d=>!d.inRange).slice(0,1).forEach(d=>chips.push({t:`⚠ ${d.axis.toLowerCase()} hors plage`,warn:true}));return chips.slice(0,5)}
function resultCard(r,i,origin=false){const el=document.createElement('div');el.className=`result-card c2-result-card ${i===0?'is-primary':''} ${origin?'c2-origin-card':''}`;const score=idxScore(r.I),name=origin?(r.profile.style||r.profile.geography||r.profile.grape):r.profile.grape;const meta=origin?`${r.profile.grape} · ${r.profile.geography||''}`:`${r.profile.style||''}${r.profile.geography?' · '+r.profile.geography:''}`;el.innerHTML=`<div class="result-top"><span class="rank">${i+1}</span><div><div class="result-name">${name}</div>${origin?`<div class="c2-origin-sub">${meta}</div>`:''}</div><span class="score">${score}</span></div><div class="bar"><span style="width:${score}%"></span></div>${origin?'':`<div class="result-meta">${meta}</div>`}${i<3?`<div class="c2-why"><div class="c2-why-title">Pourquoi ?</div><div class="c2-why-chips">${why(r).map(x=>`<span class="c2-why-chip ${x.warn?'warn':''}">${x.t}</span>`).join('')}</div></div>`:''}${i===0?'<span class="c2-engine-badge">C2-C2 · moteur release</span>':''}`;el.addEventListener('click',ev=>{ev.preventDefault();ev.stopPropagation();openProfile(r.profile,origin)});return el}
function render(){const s=filteredScore();if(!s)return;const gr=$('#grapeResults');if(gr){gr.replaceChildren(...s.grapes.slice(0,10).map((r,i)=>resultCard(r,i,false)));const c=$('#grapeCount');if(c)c.textContent='C2-C2'}const or=$('#originResults');if(or){const unique=[],seen=new Set();for(const r of s.profiles){const key=r.profile.profile_id;if(seen.has(key))continue;seen.add(key);unique.push(r);if(unique.length===10)break}or.replaceChildren(...unique.map((r,i)=>resultCard(r,i,true)))}window.__C2_LAST=s}
function topMarkers(p){return (p.markers||[]).filter(m=>m.context!=='TERTIARY_OPTIONAL').sort((a,b)=>(b.capacity||0)-(a.capacity||0))}
function openProfile(p,isOrigin){const d=$('#detailDialog'),title=$('#detailTitle'),type=$('#detailType'),body=$('#detailBody');if(!d||!body)return;type.textContent=isOrigin?'Profil origine / style':'Fiche cépage · profil C2-C2';title.textContent=isOrigin?(p.style||p.geography||p.grape):p.grape;const axes=['Acidité','Tanins','Alcool','Corps','Couleur'];const ms=topMarkers(p),ex=ms.filter(m=>m.kind==='exact').slice(0,12),gen=ms.filter(m=>m.kind!=='exact').slice(0,10);body.innerHTML=`${isOrigin?`<div class="detail-block"><b>${p.grape}</b><p>${p.geography||''}</p></div>`:`<div class="detail-block"><b>Profil affiché</b><p>${p.style||''}${p.geography?' · '+p.geography:''}</p></div>`}<div class="c2-detail-structure">${axes.map(a=>{const r=p.structure[a];return `<div class="c2-metric"><span class="c2-metric-label">${a}</span><span class="c2-metric-sat">${sat(r.center)}</span><span class="c2-metric-value">${fmt(r.center)}</span><span class="c2-metric-range">Typique [${fmt(r.L)}–${fmt(r.U)}]</span></div>`}).join('')}</div><div class="c2-detail-section"><h3>Descripteurs précis</h3><div class="c2-tags">${ex.length?ex.map(m=>`<span class="c2-tag exact">${m.item}</span>`).join(''):'<span class="muted">Aucun exact prioritaire.</span>'}</div></div><div class="c2-detail-section"><h3>Familles & signatures</h3><div class="c2-tags">${gen.map(m=>`<span class="c2-tag">${m.item}</span>`).join('')}</div></div><div class="c2-detail-section"><h3>Contexte</h3><p class="muted">Bois : ${p.native_wood??'—'} · MLF explicite : ${p.explicit_MLF?'oui':'non'} · Rg ${fmt(p.R_generic??1)}</p></div>`;try{d.showModal()}catch(_){d.setAttribute('open','')}}
function cleanupOrigin(){const tab=$('#tab-origin');if(!tab)return;const refine=[...tab.querySelectorAll('.form-card')].find(x=>/Affiner le style/i.test(x.textContent));if(refine)refine.remove();const step=tab.querySelector('.results-section .step');if(step)step.remove();const h=tab.querySelector('.results-section h2');if(h)h.textContent='Top 10 origines';}
function versionNote(){if($('#c2VersionNote'))return;const m=document.querySelector('main');if(m){const x=document.createElement('div');x.id='c2VersionNote';x.className='c2-version-note';x.textContent='Wine Blind V11 RESET · moteur C2-C2';m.append(x)}}
function wire(){cleanupOrigin();versionNote();const rerender=()=>setTimeout(render,0);document.addEventListener('click',rerender,true);document.addEventListener('change',rerender,true);document.addEventListener('pointerup',rerender,true);new MutationObserver(()=>{if(!window.__c2Rendering){window.__c2Rendering=true;setTimeout(()=>{try{render()}finally{window.__c2Rendering=false}},0)}}).observe($('#grapeResults')||document.body,{childList:true,subtree:false});render()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
})();
